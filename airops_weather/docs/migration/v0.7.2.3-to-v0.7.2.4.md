# Migration: v0.7.2.3 to v0.7.2.4

No configuration changes are required.

Replace the complete `airops_weather` resource directory and restart the resource.
The time sync payload now includes `serverUtcSecondsOfDay`; older integrations remain compatible because existing fields are unchanged.
