# Migration: v0.7.2.2 to v0.7.2.3

Version v0.7.2.3 is a drop-in self-test hotfix.

## Changes

- fixes the configuration self-test consuming the wrong return value from `AirOpsWeather.Validation.Run()`
- no configuration, API, SDK, or database changes

## Installation

Replace the complete `airops_weather` resource directory and restart the resource.
