# Migration: v0.7.1 to v0.7.2

No manual migration is required.

The client time synchronization now enforces a one-to-one clock rate and
corrects drift against the authoritative server timestamp. Existing time
overrides and exports remain compatible.
