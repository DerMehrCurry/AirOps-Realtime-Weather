# Migration: v0.7.0-beta to v0.7.0-rc

No existing export or SDK v1 method was removed.

New optional configuration sections:

- `Config.Zones`
- `Config.SelfTest`

The existing `default` zone remains the default behavior. Existing calls without
a zone continue to work unchanged.
